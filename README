# Dotfiles

The intention is to create a script that can provision everything I need on a new mac. brewfile to install stuff, set defaults, set up shell environment. The other purpose is to version control dot files in one place.
